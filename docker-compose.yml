version: '3.8'

services:
  database:
    image: postgres:14.3
    restart: always
    environment:
      POSTGRES_DB: todos
      POSTGRES_USER: todos
      POSTGRES_PASSWORD: todos-password
    ports:
      - '5432:5432'
